# Today's Challenge:
# Write a program to find the maximum and minimum values in a list.

num_list = [-4, 1, 3, 6, 10, -2]
max_num, min_num = max(num_list), min(num_list)
print(f"Max value: {max_num}")
print(f"Min value: {min_num}")
